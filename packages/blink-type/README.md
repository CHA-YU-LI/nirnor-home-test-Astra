# 隨機閃爍文字與 Logo

這是一份可直接複製的完整範例。以 HTTP 伺服器開啟 index.html；不要直接雙擊檔案，因為 ES
Module 需要 HTTP。

## 調整參數

請編輯 main.js 的 options，然後重新整理。執行中的 effect 也提供 update、replay、reset、destroy。參數細節請參考原特效頁的參數表。

本包已包含本效果的 HTML、SCSS、CSS、JavaScript 與本地共用模組；
